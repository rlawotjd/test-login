const idCheck = document.getElementById('id');
const pwCheck = document.getElementById('pw');
const pw_cCheck = document.getElementById('pw_c');
const id_c=document.getElementById('id_c')
if (idCheck.value.length>=6&&pwCheck.value.length>=8&&pwCheck.value===pw_cCheck.value) {
    
}
